Use this command to install OmicsON:

install.packages("devtools")
devtools::install_github("wjurkowski/ONION")


I case of some problem, install packages from DESCRIPTION file. Full list of packages to install:
    RCurl,
    RJSONIO,
    XML,
    pls,
    yacca,
    FRCC,
    httr,
    STRINGdb,
    BiocCheck,
    CCA,
    gridExtra,
    mygene,
    utils,
    stats
    igraph,
    plyr,
    knitr,
    rmarkdown,
    testthat

